# Database
POSTGRES_USER=tracker
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
POSTGRES_DB=tunisia_tracker

# Security - generate with: openssl rand -hex 32
SECRET_KEY=CHANGE_ME_GENERATE_RANDOM_KEY

# CORS - JSON array format (your domain, or ["*"] for any)
CORS_ORIGINS=["*"]

# API URL for frontend (empty = relative path, nginx proxies /api/* to backend)
VITE_API_URL=
